export const Challenge_4 = () => {
const sendMessage = (message) => {
  alert(message)
  }
  
  console.log(sendMessage("Мини проект!"))

const half = (number) => {
    return number / 2;
  }

  console.log(half(20))
  
const showConsole = () => {
    console.log('lincode');
  }
 
  showConsole()
  
const concatWords = (first, second) => {
    return first + second;
  }

  console.log(concatWords(10, 20))
}